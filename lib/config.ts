/**
 * Shared site configuration for SEO, metadata, and structured data.
 * Update BASE_URL when deploying to a different domain (e.g. mcpmarket.com).
 */
export const BASE_URL = "https://hot100ai.dev";
